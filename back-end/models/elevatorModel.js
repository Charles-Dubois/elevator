// dependencies
const mongoose = require("mongoose"),
  elevatorModel = mongoose.Schema({
    //Change the content with the front
  });
const Elevator = mongoose.model("elevator", elevatorModel);

module.exports = Elevator;
